# 🤖 Настройка n8n с Telegram для формы бронирования

## 📋 Что вам понадобится

- Ваш Telegram Bot Token: `8345072206:AAH43PLUrI1z2P8Zs_v_28siuGrrKygzCOc`
- Chat ID (куда отправлять сообщения)
- n8n установлен и запущен

---

## 🔍 Шаг 1: Получите Chat ID

1. Откройте Telegram и найдите вашего бота
2. Отправьте боту любое сообщение (например, `/start`)
3. Откройте в браузере:
   ```
   https://api.telegram.org/bot8345072206:AAH43PLUrI1z2P8Zs_v_28siuGrrKygzCOc/getUpdates
   ```
4. Найдите `"chat":{"id":123456789}` - это ваш Chat ID
5. Сохраните Chat ID (например, `123456789`)

---

## ⚙️ Шаг 2: Создайте Workflow в n8n

### 2.1. Создайте новый Workflow

1. Откройте n8n
2. Нажмите "Create New Workflow"
3. Назовите его "Telegram Booking Notifications"

### 2.2. Добавьте Webhook Node (Trigger)

1. Добавьте node: **Webhook**
2. Настройки:
   - HTTP Method: `POST`
   - Path: `booking` (или любой другой)
   - Authentication: `None` (API защищен на уровне Next.js)
   - Response Mode: `Last Node`

3. Скопируйте **Production URL** (например: `https://your-n8n.com/webhook/booking`)

### 2.3. Добавьте Function Node (форматирование)

1. Добавьте node: **Code** → **Run Once for All Items**
2. Вставьте следующий код:

```javascript
// Форматируем данные из формы в красивое сообщение
const data = $input.first().json;

// Функция для форматирования даты
const formatDate = (dateString) => {
  if (!dateString) return 'Не указано';
  const date = new Date(dateString);
  return date.toLocaleDateString('de-DE', {
    day: '2-digit',
    month: 'long',
    year: 'numeric'
  });
};

// Форматируем время
const formatTime = (time) => {
  return time || 'Не указано';
};

// Создаем красивое сообщение с эмодзи
const message = `
🆕 <b>НОВАЯ ЗАЯВКА НА ЗАПИСЬ</b>

👤 <b>Клиент:</b>
   ${data.vorname} ${data.nachname}

📞 <b>Телефон:</b>
   <code>${data.telefon}</code>

📧 <b>Email:</b>
   ${data.email}

💅 <b>Услуга:</b>
   ${data.leistung || 'Не указано'}

📅 <b>Желаемая дата:</b>
   ${formatDate(data.wunschtermin)}

🕐 <b>Желаемое время:</b>
   ${formatTime(data.wunschuhrzeit)}

📝 <b>Комментарий:</b>
   ${data.nachricht || 'Нет комментария'}

⏰ <b>Получено:</b>
   ${new Date(data.submittedAt).toLocaleString('de-DE')}

━━━━━━━━━━━━━━━━━━━━━━
🌐 Источник: ${data.source || 'Website'}
`;

return [{
  json: {
    message: message,
    chat_id: data.chat_id || null
  }
}];
```

3. Соедините Webhook → Function

### 2.4. Добавьте Telegram Node

1. Добавьте node: **Telegram**
2. Настройки:
   - **Credential**: создайте новый Credential
     - Name: `Telegram Bot`
     - Access Token: `8345072206:AAH43PLUrI1z2P8Zs_v_28siuGrrKygzCOc`
   - **Resource**: `Message`
   - **Operation**: `Send Message`
   - **Chat ID**: введите ваш Chat ID из Шага 1
   - **Text**: `={{ $json.message }}`
   - **Parse Mode**: `HTML`

3. Соедините Function → Telegram

### 2.5. Добавьте Respond to Webhook Node

1. Добавьте node: **Respond to Webhook**
2. Настройки:
   - **Response Body**: JSON
   - **Response Data**: JSON

3. В поле JSON добавьте:
```json
{
  "success": true,
  "message": "Ihre Buchungsanfrage wurde erfolgreich gesendet!"
}
```

4. Соедините Telegram → Respond to Webhook

### 2.6. Активируйте Workflow

1. Нажмите **Activate** (переключатель в правом верхнем углу)
2. Скопируйте Production Webhook URL

---

## 🔐 Шаг 3: Настройте переменные окружения

1. На вашем сервере или в Vercel добавьте переменную:
   ```
   N8N_WEBHOOK_URL=https://your-n8n.com/webhook/booking
   ```

2. Для локальной разработки создайте `.env.local`:
   ```bash
   N8N_WEBHOOK_URL=https://your-n8n.com/webhook/booking
   ```

---

## 🧪 Шаг 4: Протестируйте интеграцию

### Тест через curl:
```bash
curl -X POST https://your-n8n.com/webhook/booking \
  -H "Content-Type: application/json" \
  -d '{
    "vorname": "Maria",
    "nachname": "Müller",
    "telefon": "+49 176 12345678",
    "email": "maria@example.com",
    "leistung": "Klassische Fußpflege",
    "wunschtermin": "2025-02-15",
    "wunschuhrzeit": "14:00",
    "nachricht": "Ich hätte gerne einen Termin",
    "submittedAt": "2025-01-15T10:30:00Z",
    "source": "website"
  }'
```

### Ожидаемый результат:
Вы должны получить в Telegram красиво отформатированное сообщение! 🎉

---

## 📱 Как будет выглядеть сообщение в Telegram:

```
🆕 НОВАЯ ЗАЯВКА НА ЗАПИСЬ

👤 Клиент:
   Maria Müller

📞 Телефон:
   +49 176 12345678

📧 Email:
   maria@example.com

💅 Услуга:
   Klassische Fußpflege

📅 Желаемая дата:
   15. Februar 2025

🕐 Желаемое время:
   14:00

📝 Комментарий:
   Ich hätte gerne einen Termin

⏰ Получено:
   15.01.2025, 11:30:00

━━━━━━━━━━━━━━━━━━━━━━
🌐 Источник: website
```

---

## 🎯 Дополнительные возможности

### Добавить кнопки для быстрых действий:

В Telegram Node добавьте Reply Markup:
```json
{
  "inline_keyboard": [
    [
      {"text": "✅ Подтвердить", "callback_data": "confirm"},
      {"text": "❌ Отклонить", "callback_data": "decline"}
    ],
    [
      {"text": "📞 Позвонить", "url": "tel:+491761234567"}
    ]
  ]
}
```

### Отправлять в разные чаты в зависимости от услуги:

В Function Node добавьте логику:
```javascript
const chatIds = {
  'Klassische Fußpflege': '123456789',
  'Smart-Pediküre': '987654321',
  'default': '123456789'
};

const chatId = chatIds[data.leistung] || chatIds['default'];
```

---

## ⚠️ Важные замечания

1. **Безопасность**:
   - Никогда не храните Bot Token в коде
   - Используйте переменные окружения
   - Включите Rate Limiting на webhook

2. **Тестирование**:
   - Сначала протестируйте через n8n Test Webhook
   - Затем проверьте через форму на сайте

3. **Мониторинг**:
   - Проверяйте логи n8n на наличие ошибок
   - Настройте уведомления об ошибках

---

## 🆘 Решение проблем

### Сообщения не приходят:
1. Проверьте, что workflow активирован
2. Убедитесь, что Bot Token правильный
3. Проверьте Chat ID
4. Посмотрите логи выполнения в n8n

### Форматирование не работает:
1. Убедитесь, что Parse Mode = `HTML`
2. Проверьте валидность HTML тегов
3. Экранируйте специальные символы

### 403 Forbidden:
1. Убедитесь, что бот не заблокирован
2. Отправьте боту `/start`
3. Проверьте права бота

---

## 📞 Контакты для поддержки

Если возникнут вопросы, проверьте:
- [n8n Documentation](https://docs.n8n.io)
- [Telegram Bot API](https://core.telegram.org/bots/api)

Удачи! 🚀
